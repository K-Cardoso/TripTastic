<!--([@destination, @destination.trips.build])  |  form_for ([@article, @article.comments.build]) do |f| -->
<%= form_for :trip, url: destination_trips_path(@destination) do |f| %>
	<p>
		<%= f.label :depart_date %><br>
		<%= f.date_field :depart_date %>
	</p>
	<p>
		<%= f.label :return_date %><br>
		<%= f.date_field :return_date %>
	</p>
	<p>
		<%= f.label :depart_time %><br>
		<%= f.time_field :depart_time %>
	</p>
	<p>
		<%= f.label :return_time %><br>
		<%= f.time_field :return_time %>
	</p>
	<p>
		<%= f.label :mode %><br>
		<%= f.select(:mode, options_for_select(
			[['Airplane','AIR'],['Car','CAR'],['Bus','BUS'],['Train','TRAIN']]))%>
	</p>
	<p>
		<%= f.submit%>
	</p>
<% end %>